# Regras do Jogo

Esta página documenta as regras implementadas em `core/rules.py`.

::: core.rules.get_valid_moves

::: core.rules.opponent

::: core.rules.piece_color


## Sumário

- `get_valid_moves(board, color)`: retorna uma lista de movimentos válidos para o jogador.
- Regras de captura múltipla e promoção estão implementadas conforme o padrão de damas.
